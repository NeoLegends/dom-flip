{
  "name": "dom-flip",
  "version": "0.2.0",
  "description": "FLIP move for web components",
  "main": "dist/dom-flip.js",
  "repository": "https://github.com/Festify/dom-flip.git",
  "author": "Moritz Gunz <moritz.gunz@gmail.com>",
  "license": "MIT",
  "private": false,
  "scripts": {
    "build": "yarn lint && NODE_ENV=production tsc",
    "fix": "tslint -p tsconfig.json --fix",
    "lint": "tslint -p tsconfig.json"
  },
  "dependencies": {},
  "devDependencies": {
    "tslint": "^5.9.1",
    "typescript": "^2.6.2"
  }
}
