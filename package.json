{
  "name": "dom-flip",
  "version": "0.4.1",
  "description": "Smooth position animation for web components",
  "main": "dist/dom-flip.js",
  "repository": "https://github.com/NeoLegends/dom-flip.git",
  "bugs": "https://github.com/NeoLegends/dom-flip/issues",
  "author": "Moritz Gunz <moritz.gunz@gmail.com>",
  "keywords": [
    "animation",
    "FLIP",
    "move",
    "magic-move",
    "web components"
  ],
  "license": "MIT",
  "private": false,
  "scripts": {
    "build": "yarn lint && NODE_ENV=production tsc",
    "fix": "tslint -p tsconfig.json --fix",
    "lint": "tslint -p tsconfig.json"
  },
  "devDependencies": {
    "tslint": "^5.9.1",
    "typescript": "^2.6.2"
  },
  "files": [
    "dist/*.{js,d.ts,js.map}"
  ]
}
